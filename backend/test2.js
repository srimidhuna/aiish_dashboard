const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const res = await prisma.followUp.create({ data: { babyId: 'seed-baby-0001', followUpType: 'regular', status: 'scheduled', scheduledDate: new Date('2026-07-30T00:00:00Z'), providerId: '2845c7ce-c815-4eff-ab13-556a7a3e01b7' } }); console.log('success', res.id); } catch(e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
