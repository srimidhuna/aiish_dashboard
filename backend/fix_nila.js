const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const child = await prisma.baby.findFirst({ where: { mrNumber: '456789' } }); if (child) { const res = await prisma.followUp.create({ data: { babyId: child.id, followUpType: 'regular', status: 'scheduled', scheduledDate: new Date('2026-07-30T00:00:00Z'), providerId: child.createdById, notes: 'Restored missing follow-up from failed registration.' } }); console.log('Follow-up created for Nila:', res.id); } } catch(e) { console.error(e); } finally { await prisma.$disconnect(); } } main();
