# Base URL of the backend API — must be browser-accessible (not Docker service name)
# The frontend is a client-side SPA; API calls go from the browser, not the container.
VITE_API_URL=http://localhost:3001
